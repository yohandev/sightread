/// opens a `.midi` file, given the start, `ptr`, of its encoded
/// data and how many bytes it is.
/// 
/// returns a pointer to the `MidiFile` object, to be used with
/// other midi methods.
pub fn open_midi_file(ptr: u32, len: u32) -> u32
{
    todo!()
}